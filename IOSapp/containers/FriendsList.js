import React from 'react-native';

const {
  Component,
  View,
  ScrollView,
  Text,
} = React;

class FriendsList extends Component {
  render() {
    return (
      <ScrollView>
        <View>

        </View>
      </ScrollView>
    );
  }
}

export default FriendsList;
